R := RealField();
C := ComplexField();

zero := R!0;
one := C!1;
i := elt<C | 0, 1>;
point5 := elt<R | 5, -1>;
half := one/2;
epsilon := elt<R | 1, -20>;

Q := [C | zero, one, half, 5/7, point5, elt<R | 355/113, 0>, i, -half + i * Sqrt(R!2) / 2 ];

