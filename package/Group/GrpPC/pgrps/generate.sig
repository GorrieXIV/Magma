174,1
V,GeneratepGroups,2
S,DefinitionSets,"return s-step definition sets and offsets of dimension n in vector space of dimension m, where we must retain the first Fixed basis elements",0,4,0,0,0,0,0,0,0,148,,0,0,148,,0,0,148,,0,0,148,,82,82,148,-38,-38,-38
S,Descendants,"Construct descendants of p-group having p-class at most ClassBound, and order at most p^OrderBound; if Exponent is non-zero, generate descendants of G satisfying this exponent law; if StepSizes is supplied, then construct descendants of order p^(n + s) of a group of order p^n, only for s in StepSizes; by default, all descendants of G are returned; if MaxAuts is true, then construct maximally automorphic descendants; if All = false, only capable ones are returned; also return total number of descendants",0,2,0,0,0,0,0,0,0,148,,0,0,129,,82,148,-38,-38,-38,-38
S,Descendants,"Construct descendants of p-group having p-class one larger than G, and order at most p^OrderBound; if Exponent is non-zero, generate descendants of G satisfying this exponent law; if StepSizes is supplied, then construct descendants of order p^(n + s) of a group of order p^n, only for s in StepSizes; by default, all descendants of G are returned; if MaxAuts is true, then construct maximally automorphic descendants; if All = false, only capable ones are returned; also return total number of descendants",0,1,0,0,0,0,0,0,0,129,,82,148,-38,-38,-38,-38
S,GeneratepGroups,"Generate tree of d-generator p-class <= c p-groups of order at most p^OrderBound; if Exponent is non-zero, generate groups satisfying this exponent law; if StepSizes is supplied, then construct extensions of order p^(n + s) of a group of order p^n only for s in StepSizes; by default, all groups satisfying these properties are returned; if MaxAuts is true, then construct maximally automorphic descendants; if All = false, only capable ones are returned; also return total number of groups",0,3,0,0,0,0,0,0,0,148,,0,0,148,,0,0,148,,82,148,-38,-38,-38,-38
