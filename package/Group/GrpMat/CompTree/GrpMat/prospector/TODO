- Compare statistical tests, save stopping points when they
  accept. Compare stopping points (number of steps) against each
  other.

Lawrence:
- emphasize more that we use both standard chi^2 and homogeneity
- use stat tables for chi^2 in experiments, since batch size is fixed
