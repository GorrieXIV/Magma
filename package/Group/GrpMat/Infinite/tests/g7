G :=MatrixGroup<7, K | [
    DiagonalMatrix(K, [ w, w^2, 1, 1, 1, 1, 1 ])
]> where w := K.1 where K := ext<K|Polynomial(K, [-2, 0, 1])> where K is RationalField()
;

IsCompletelyReducible (G);
IsSolubleByFinite (G);
HirschNumber (G);


