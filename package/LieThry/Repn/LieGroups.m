freeze;

/*
intrinsic Center( G::GrpLie ) -> SetIndx
{ Returns the Center of the specified group }
  return RationalPoints(Scheme(AffineSpace(BaseRing(G), Rank(G)), Setseq(CenterPolynomials(G))));
end intrinsic;
*/
